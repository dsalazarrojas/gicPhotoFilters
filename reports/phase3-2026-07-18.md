# Phase 3 shell evidence — SHL-301 to SHL-308

## Completed

- Home now owns the trend launcher and renders all active `viralTags` trends at `/#trends`.
- `trends.html` is a compatibility redirect to `/#trends`.
- `browse.html` is the single full-catalog surface and is labeled for the complete 200+ catalog.
- Primary navigation exposes only Browse and Advanced; Build/BYOK are not primary-nav items.
- `advanced.html` groups BYOK settings, Builder, and health detail.
- Home hero has one primary CTA, resolved to the highest-priority active trend at runtime.
- Root `/assets/` was confirmed to have zero live HTML/runtime references, then removed.
- Category detail cards use the exact `Try this filter` CTA and link into `/try.html?id=...`; 15 category pages are present.
- Four legacy PRDs carry a superseded banner. Stale `.claude/` planning docs are archived under `docs/archive/claude/`. `.DS_Store` is ignored and the found non-generated copies were removed.

## Gates

- Static local-link audit: PASS — no dead local HTML or asset links.
- Root `/assets/` reference audit: PASS — zero references outside historical PRD text.
- JavaScript syntax and `git diff --check`: PASS.
- Preview generator: executed with `node scripts/generate-previews.js --batch 1 --budget 1 --delay-ms 0 --timeout-ms 30000`. The remote request failed with `fetch failed` because no preview/BYOK endpoint was available in this environment; the incomplete generated source file was removed.
- Review pipeline: inspected with `python3 scripts/review_generated_filters.py --help`; it requires generated queue items and an interactive review server. No generated pair was available to accept.

## Remaining external gate

The golden set has 20 unique filters (four active-trend filters plus the top-20 popularity set). Existing real preview pairs cover 17; `grinch-ify`, `superhero`, and `anime-portrait` still need successful generator output and human acceptance in the review UI before SHL-306 can be marked 100%.
